# Core UI
streamlit>=1.33,<2
pillow>=10.3,<11

# Core math
numpy==2.0.2

# TTS (works with espeak-ng on Linux; add espeak-ng in packages.txt)
pyttsx3>=2.99

# Paddle stack (Py 3.13 compatible)
paddlepaddle==3.1.0
paddleocr==3.1.0
